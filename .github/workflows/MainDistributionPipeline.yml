#
# This workflow calls the main distribution pipeline from DuckDB to build, test and (optionally) release the extension
#
name: Main Extension Distribution Pipeline
on:
  push:
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: "0 2 * * *" # Runs every night at 02:00 UTC

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref || '' }}-${{ github.base_ref || '' }}-${{ github.ref != 'refs/heads/main' || github.sha }}
  cancel-in-progress: true

jobs:
  duckdb-stable-build:
    name: Build extension binaries
    uses: rustyconover/extension-ci-tools/.github/workflows/_extension_distribution.yml@ed9599e34b6b74131d70e2963c94b4b6e3cf567c
    with:
      duckdb_version: v1.4-andium
      ci_tools_version: ed9599e34b6b74131d70e2963c94b4b6e3cf567c
      override_ci_tools_repository: rustyconover/extension-ci-tools
      extension_name: lindel
      enable_rust: true
